# Platform files

Platform-specific launchers and packaging notes live here. The game content is
shared by all platforms and remains in the project root.

- `linux/` contains the Linux launcher.
- `windows/` contains the Windows launcher.
- `android/` contains Android packaging notes and future build assets.