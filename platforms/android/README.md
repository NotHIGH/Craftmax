# Android

Android requires a Luanti Android build. Craftmax is already structured as a
game package, so the game directory can be bundled or copied into the Android
Luanti games directory.

The Android client and Gradle project are not part of this repository yet.
When Android packaging starts, keep platform build files here and keep shared
game content in the project root.